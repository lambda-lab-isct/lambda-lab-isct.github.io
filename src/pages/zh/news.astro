---
import NewsList from "@/components/NewsList.astro";
import PageHero from "@/components/PageHero.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";
import { pages } from "@/config/site";
import { byDateDesc, getDataCollection } from "@/utils/content";

const locale = "zh";
const page = pages.news;
const news = byDateDesc(await getDataCollection("news"));
---

<BaseLayout
  locale={locale}
  page="news"
  title={page.title.zh}
  description={page.description.zh}
  robots="noindex, follow"
>
  <PageHero title={page.title.zh} description={page.description.zh} />
  <section class="section">
    <div class="container">
      <NewsList items={news} locale={locale} />
    </div>
  </section>
</BaseLayout>
