---
import PageHero from "@/components/PageHero.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";
import { pages, wctpArchiveLinks } from "@/config/site";

const locale = "ja";
const page = pages.wctp;
---

<BaseLayout
  locale={locale}
  page="wctp"
  title={page.title.ja}
  description={page.description.ja}
>
  <PageHero title={page.title.ja} description={page.description.ja} />
  <section class="section">
    <div class="container">
      <h2>WCTPアーカイブ</h2>
      <ul class="list-plain">
        {
          wctpArchiveLinks.map((archive) => (
            <li class="card">
              <h3>
                <a href={archive.path}>{archive.label.ja}</a>
              </h3>
            </li>
          ))
        }
      </ul>
    </div>
  </section>
</BaseLayout>
